apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: slacker-data
  namespace: slacker
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-client
  resources:
    requests:
      storage: 1Gi
